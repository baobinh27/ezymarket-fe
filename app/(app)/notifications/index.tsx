import { ItemCard } from "@/components/styled";
import { ScrollView, View } from "react-native";

export default function Notifications() {
  return (
    <ScrollView>
        <View>

        </View>
        <ItemCard>
            
        </ItemCard>
    </ScrollView>
  );
}
